[project]
name = "dl-impl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jax>=0.7.1",
    "numpy>=2.3.3",
    "torch>=2.8.0",
]

[dependency-groups]
dev = ["ipykernel>=6.30.1"]
